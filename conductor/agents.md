# Agent Instruction: o365-cli Context & Standards

This document serves as the primary entry point for ALL AI agents (Claude, Gemini, Cursor, Windsurf, VS Code Copilot) working on the **o365-cli** project.

## 🎯 Core Mission
You are an expert technical partner assisting in the development of a high-performance Microsoft 365 administration toolkit.

## 📚 Source of Truth
Before performing any task, you **MUST** read the following documents in order:
1.  **`conductor/tech-stack.md`**: Architectural constraints and allowed tools.
2.  **`conductor/product.md`**: Vision and feature pillars.
3.  **`conductor/workflow.md`**: The strict TDD and implementation lifecycle.

## ⚙️ Technology Stack Standards
-   **Primary:** **TypeScript** running on **Bun**. This is the default for all orchestration and core logic.
-   **Secondary:** **PowerShell 7+**. Supported for specific scripts, Exchange Online tasks, or if the user explicitly requests it.
-   **Runtime:** Always prefer `bun` over `npm` or `node`.

## 📁 Project Structure
-   **`core/`**: The modern TypeScript/Bun platform.
    -   `src/cli.ts`: Main entry point.
    -   `src/commands/`: Module-specific logic.
    -   `src/services/`: Shared infrastructure (Auth, Graph, Storage).
-   **`legacy/`**: Archived PowerShell modules (Reference only).
-   **`conductor/`**: The "Conductor" framework docs and active development tracks.

## 🤖 Interaction Protocol
-   **TDD First:** Always write failing tests in `core/src/` (using `bun test`) before implementing.
-   **Dry-Run by Default:** All destructive or administrative actions must support and default to a `--dry-run` mode.
-   **Stay in Sync:** Follow the `plan.md` within the active track folder in `conductor/tracks/`.

---
*Generated by Conductor - Stack Consolidation Track 2026-01-13*
